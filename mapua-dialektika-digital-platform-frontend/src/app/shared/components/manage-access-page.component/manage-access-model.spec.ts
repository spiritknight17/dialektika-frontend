import { ManageAccessModel } from './manage-access-model';

describe('ManageAccessModel', () => {
  it('should create an instance', () => {
    expect(new ManageAccessModel()).toBeTruthy();
  });
});
